function calNum(num1: number, num2: number): number {
  return num1 + num2;
}

function concatStr(str1: string, str2: string): string {
  return str1 + str2;
}

console.log(calNum(1, 2));
console.log(concatStr("Helle", "world"));
